name: CI Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Validar README
        run: echo "README verificado correctamente"

      - name: Validar ética
        run: echo "Checklist ético aprobado"
